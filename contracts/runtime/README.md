# Runtime Contracts

The first contract is `runtime.execution` v1.0.0.

Current state: **proposed**.

It becomes adopted only after the runtime implementation and its observable behavior are independently checked against the contract and the result is recorded as evidence.
